{
  "name": "techhaven-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "TechHaven Agent Gateway（P1 骨架，TH-RFC-001 §05.1）— 引擎生命周期 / 会话管理 / 事件桥（SSE）/ 权限中继 / 基础配额",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test \"src/**/*.test.ts\"",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "load": "tsx scripts/load-events.ts",
    "reconcile": "tsx scripts/reconcile-events.ts",
    "db:migrate": "tsx scripts/migrate-agent-db.ts",
    "smoke": "npm run build && tsx src/smoke.ts",
    "smoke:pg": "npm run build && tsx src/smoke.pg.ts"
  },
  "dependencies": {
    "pg": "^8.13.1",
    "techhaven-contracts": "file:../../contracts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/pg": "^8.11.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
